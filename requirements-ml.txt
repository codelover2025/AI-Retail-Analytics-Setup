# Production face detection (install AFTER requirements.txt)
# On Windows: use Python 3.11 or 3.12, or install MSVC Build Tools (see below)
-r requirements.txt

insightface>=0.7.3
onnxruntime>=1.20.0
